# TiDB Cloud Zero — https://zero.tidbcloud.com/ (provision via browser or API v1beta1)
# Production: claim the instance before ~30d TTL so credentials stay valid.
# Same URL can power HiClaw service + Vercel (/admin/tidb, webhook/onchain, /api/reputation).
TIDB_DATABASE_URL="mysql://user:password@gateway...tidbcloud.com:4000/expert_network?sslaccept=strict"

# DashScope (Alibaba Cloud) — Qwen-Max for shadow worker generation
DASHSCOPE_API_KEY="sk-your-dashscope-key"

# mem9 — persistent expert memory (no API key needed, uses space IDs)
MEM9_ENABLED="true"
